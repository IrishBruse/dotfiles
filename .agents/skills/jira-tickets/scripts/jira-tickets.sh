#!/usr/bin/env bash
# Emit Jira issue lines for the skill body (stdin = fenced block from SKILL.md).
# When stdin is empty, prints a stub. Wire jira-cli here when available.
set -euo pipefail

echo "NOVACORE-54646, NOVACORE-4564"
