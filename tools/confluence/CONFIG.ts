// --- Edit CONFIG (only source of settings) ---
export const CONFIG = {
  /** Host only, e.g. "your-org.atlassian.net" */
  site: "your-org.atlassian.net",
  /** Atlassian cloud id from acli confluence profile */
  cloudId: ""
} as const;
