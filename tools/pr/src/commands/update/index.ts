export function runUpdate(args: string[]): void {
  console.log("pr update: running (skeleton)");
  if (args.length > 0) {
    console.log("pr update: extra args (ignored for now):", args.join(" "));
  }
}
