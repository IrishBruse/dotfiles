# VS Code integrated terminal passes Ctrl+V to fish (see vscode/keybindings/custom.json).
# Ghostty handles Ctrl+V before fish (see ghostty config).
bind \cv paste_clipboard
bind \x16 paste_clipboard
