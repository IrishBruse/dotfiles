---
description: Skill authoring standards for .cursor/skills and .agents/skills
globs: **/.cursor/skills/**,**/.agents/skills/**
alwaysApply: false
---

# Skill authoring

Load `writing-great-skills` when authoring or editing a skill.

## Frontmatter description

Write `description` as a plain short string with a minimal usage example.
Use a normal YAML string, not block scalars (`>`, `|`, `>-`, `|-`, or variants).

Good:

```yaml
description: Create or update a GitHub pull request title and body from the current branch. Use when opening or updating a pull request.
```

Weak:

```yaml
description: >
  Create or update a GitHub pull request title and body from the current branch.
  Use when opening or updating a pull request.
```

## Self-contained

Keep each skill self-contained.
Pull in or duplicate another skill's guidance only when the user asks to align with it, or when extending linkage the skill already has.
Write rules directly, skip provenance or mirroring labels on sections.

Reference other skills by name in backticks (`skill-name-here`), not by file path.
In repo skills, reference only skills that ship with that repo, not personal global skills other teammates may lack.

## Steps

Use `1.`, `2.`, ... only for short sequences where each item fits on one line.
When items wrap or the list grows, break into `### Step 1` headings with detail in paragraphs underneath.

Good:

```markdown
### Step 1

Confirm scope with the user.
Stop and wait for a reply before continuing.

### Step 2

Write `plan.md` from the confirmed scope.
```

Weak:

```markdown
1. Confirm scope with the user. Stop and wait for a reply before continuing.
2. Write `plan.md` from the confirmed scope.
3. Implement only after the user approves the plan.
4. Run the quality gate before finishing.
5. Update `plan.md` if decisions change during implementation.
```
