---
alwaysApply: true
---

**Users Name:** Ethan Conneely

# Rules

- No emojis. Example: 😀
- Always prefer `,` over `;` in english text.
- Always use plain ASCII no Unicode. Example: no em/en dash.
- Always run a full quality gate before finishing.
- If you run into an auth issue dont try and get around it ask the user.
- If Jira MCP is not working stop and tell the user to login.

## Code 

- Dont create a file for just 1 or 2 functions there needs to be a justification. Example: reuse thats needed not a possible reuse in the future.
- In markdown files always add a newline `\n` after a `.` instead of making a very long single line.
