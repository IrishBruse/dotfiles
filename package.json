{
  "name": "dotfiles",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tools/tsconfig.json && tsc --noEmit -p vscode/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^5.8.3"
  }
}
